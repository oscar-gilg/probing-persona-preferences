# Qwen-3.5-122B steering layer-sweep + harm-breakdown — running log

## 2026-05-05

### Setup
- Spec finalised in main session; full review pass via `zombuul:review-spec`.
- Pair JSONs built fresh from disjoint pool (706/1000 canonical_test, zero leakage with 10k AL probe-train): `steering_pairs_50.json` (50 pairs, 25 strata) + `steering_pairs_150.json` (50 bb / 50 hb / 50 hh).
- 12 Phase A YAMLs generated by `gen_configs.py`, norms inlined per (selector, layer).
- Worktree at `.claude/worktrees/steering_layer_sweep`, branch `worktree-steering_layer_sweep` pushed.

### Pod
- Created `qwen-steering-sweep` (`dyl8s7sbv04asp`): 2× A100-SXM4-80GB, 500GB disk, 50GB volume.
- SSH alias `runpod-qwen-steering-sweep` configured.
- Pod setup: base preferences install (no `[serving]` extra — vllm pin mismatch in upstream pyproject; we don't need vllm). `requests` installed manually after first run failed.
- Probes (m1, m4 manifests + L{12,24,28,33,38,43} weights) rsync'd to pod.
- .env synced.

### Phase A positive control
- Config: `configs/steering/qwen_layer_sweep/positive_control_tb1_L38.yaml` (5 pairs, 1 trial, c=+0.05 only).
- First run failed with `ModuleNotFoundError: requests`. Fixed; re-launched.
- Pass criterion: P(chose steered) > 0.6 on 5 pairs.
