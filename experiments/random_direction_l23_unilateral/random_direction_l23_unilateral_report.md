# Random-direction L23 unilateral control

**Status:** in progress (executing on pod).

Spec: `random_direction_l23_unilateral_spec.md` — single-task null for L23 random directions, 5 seeds, on the first 30 pairs of the harm breakdown set.

## Headline

_TBD — pending steering runs across seeds {0, 1, 2, 3, 42}._

## Plots

_TBD._

## Methodology

See spec. Single-task, contrastive injection mode (`spans: {first: 1}` and `spans: {second: 1}`), coefficients ±0.05, ±0.03, 0; 30 pairs × 5 coefs × 2 conditions × 3 trials = 900 generations per seed; 5 seeds total = 4500 generations.
