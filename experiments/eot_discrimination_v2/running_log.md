# EoT discrimination v2 — running log

## 2026-05-01

**Setup**
- Spec: `experiments/eot_discrimination_v2/eot_discrimination_v2_spec.md` (reviewed by `/zombuul:review-spec`, fixes applied).
- Branch: `eot_discrimination_v2` (off main).
- Corpora: truth 1000 / harm 1000 / politics 795 — committed to branch with assistant-turn variants pre-generated by gemini-3-flash-preview.
- Pod: `runpod-eot-discrim-v2` (4×A100 80GB SXM, ID `zybc8uf8vgj01s`, IP 64.247.206.116:11601). Container disk 500GB / volume 50GB.
- Pod-side: branch checked out, project deps installed via `uv pip install -e ".[dev]"` (the `serving` extra was the only failure — not needed here), .env synced, all 5 probe-weight dirs rsynced.
- Mode: local — drive scoring on the pod via SSH from this session; pod is an SSH target only.

**Cost so far** (rough)
- Sonnet 4.6 + GPT-5 + Gemini 2.5 Pro on harm/politics LLM-agreement filters: ~$15-20.
- Gemini 3 Flash Preview for assistant-turn LLM completions (~2800 calls with retries): ~$2.
- Pod time: ~10 min of A100×4 so far (~$3).

## Pipeline order

1. Build scoring inputs (local CPU): cross corpora × sysprompts × turns → `scoring_inputs/{user|assistant}_turn.json`. **DONE** — 7000 user-turn + 8571 assistant-turn records.
2. Sync inputs to pod. **DONE** (tracked in git).
3. Run Gemma scoring (cuda:0, batch=16) in tmux. Pilot passes; scoring at ~100 rec/sec.
4. Run Qwen scoring (device_map=auto across 4 GPUs, batch=4). Sequential after Gemma.
5. rsync scoring outputs back to local.
6. Regenerate the 5 §3.1 figure scripts with the v2 data paths.
7. Refresh `numbers.tex` claims.
8. Pause pod (auto via babysit cron `cc545e4c`).

## Pilot (Gemma user-turn neutral, batch=16)

Last 5 tokens of formatted prompt for 5 sample items: `[' a', ' scientist', '.', '<end_of_turn>', '\n']` — confirms `<end_of_turn>` is present in last 5 tokens; `scores_arr[-1]` is the trailing `'\n'` (matches v1 convention). Probe score ranges look reasonable (tb-2_L32 across pilot: -11.6 to +2.5).

Throughput at batch=16 on 1×A100: ~91-117 records/sec. Estimated total wall-clock ~1 h (vs 3-4 h with per-item scoring).
