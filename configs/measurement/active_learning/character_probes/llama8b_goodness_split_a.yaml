preference_mode: pre_task_active_learning
backend: vllm

model: /path/to/models/llama-3.1-8b-goodness
temperature: 1.0

n_tasks: 1000
task_origins: [wildchat, alpaca, math, bailbench, stress_test]
include_task_ids_file: configs/measurement/active_learning/mra_exp2_split_a_1000_task_ids.txt

templates: src/measurement/elicitation/prompt_templates/data/completion_preference.yaml

response_formats: [completion]
n_samples: 5
pair_order_seed: 42

active_learning:
  initial_degree: 4
  batch_size: 500
  max_iterations: 10
  p_threshold: 0.3
  q_threshold: 0.3
  convergence_threshold: 0.99
  seed: 42
