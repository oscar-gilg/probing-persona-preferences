# Paper issues flagged by claim audit

Discrepancies between main.tex prose and the data-backed claims in
`paper/claims.md`. Each item is a place where switching to a macro would
*change* what the paper asserts, so it needs user judgement.

## Open

| Where | Paper prose | Data / macro | Notes | Flagged |
|---|---|---|---|---|
| App §B line 551 (persona sweep PCA) | "Within each cluster pairwise r ranges from 0.56 to 0.81" | `\personaSweepWithinClusterRMinimum` = 0.45, `\personaSweepWithinClusterRMaximum` = 0.81 | Within-cluster min = 0.45 (narcissist ↔ contrarian, dark/oppositional cluster). The 0.56 figure appears to track the max in-set abs-r of the final-six set, not the 16-persona within-cluster min. | persona-sweep subagent, 2026-04-24 |
| I-2026-05-04-1: paper/claims/canonical_probe_eval_make_paper_figures.json — `source` points at a producer that no longer reproduces the values (NOT a content issue) | 14 scalar claims (harm `\|d\|`, truth `d`, n counts, all 5 truth-personas, all 5 harm-personas, all 9 politics-personas, the 7 induced-shift scalars) carry `source: experiments/token_level_probes/canonical_probe_eval/scripts/make_paper_figures.py` but were hand-updated to v2 values in commit `0df56e89` (2026-05-04). Per `scripts/paper/refresh_eot_v2_claims.py` docstring: "Scalar claims for base discrimination (already updated by hand) are left alone." | Sidecar, `numbers.tex`, and `main.tex` are consistent (creakTruthCohensD=1.9, bailbenchHarmNItemsProse=500, etc., all match the v2 rerun in `experiments/eot_discrimination_v2/`). The risk is forward-only: re-running the listed `make_paper_figures.py` will OVERWRITE the v2 values with stale v1 numbers (it still reads the old `system_prompt_modulation_v2/parent_eot_scores.json`). **Fix:** either (a) flip the 14 claims' `source` to `manual:` and remove their `register(...)` calls from `make_paper_figures.py`, or (b) extend `refresh_eot_v2_claims.py` to recompute them from `experiments/eot_discrimination_v2/scoring/` and re-register. The 4 structured claims (Persona modulation d full, Per turn parent absolute d, Per turn absolute d truth/harm) already have a working v2 producer. | claim-audit, 2026-05-04 |
| I-2026-05-04-2: paper/claims/cross_persona_unilateral.json — producer broken since v2 migration | All 13 claims. Producer `scripts/paper/claims/compute_cross_persona_unilateral_claims.py` reads from `experiments/cross_persona_unilateral/checkpoints/{persona}.parsed.jsonl`; commit `6f236f2a` (2026-05-04, "cross-persona steering v2: switch to Assistant probe across all personas") moved the per-persona-probe data to `checkpoints_v1_perprobe/` and gitignored it. | Recorded values are arithmetically correct against the v1 (per-persona probe) archive, but: (a) the producer's hardcoded path no longer matches; (b) the experiment description has migrated to v2 (Assistant probe) — claim statements still describe v1. **Decision needed:** either (i) fix the producer path to `checkpoints_v1_perprobe/` and keep claims describing v1, or (ii) re-run v2 with the Assistant probe and re-register against the new data. | claim-audit subagent, 2026-05-04 |
| I-2026-05-04-3: paper/claims/plot_cross_model_bar.json — stale data_paths/derivation (FAIL-statement) | All 6 claims list `data_paths` and `derivation` referencing `pooled_metrics.json`, but the producer reads `pooled_metrics_clean.json` (separate_run block). | Values still correct; only the provenance string is misleading. Fix in the producer's `register(...)` calls. | claim-audit subagent, 2026-05-04 |
| I-2026-05-04-4: paper/claims/qwen_e1c_avc.json — DATA_MISSING (5 claims) | Producer requires `.claude/worktrees/qwen_replication/` (activations + pairwise.json + L38 probe); not on local clone. | Cannot audit until worktree synced. Source field claims this is a real producer (not manual:), but it cannot run on this clone — either sync the worktree or update source to `manual:`. | claim-audit subagent, 2026-05-04 |
| I-2026-05-06-1: paper/claims/core_prose_claims.json — `Gemma steering probe layer` claims L25, paper uses L23 | Claim value `\gemmaSteeringProbeLayer = 25`; statement says "Gemma-3-27B contrastive steering is applied at residual-stream layer 25". Paper main.tex uses L23 throughout the canonical steering setup: §sec:method-val2 line 120 ("When steering at layer 23 in Gemma..."), Fig.~\ref{fig:steering} caption line 125 ("(L23)"), §App.~steering-causal-window line 698 ("peaking at L23"), single-task line 707 ("...at L23"), cross-persona Fig.~\ref{fig:cross-persona-steering} caption line 714 ("...at L23"). L25 only appears in (a) safety-override fig:safety-override caption line 417, (b) open-ended sadist-steering footnote line 721 ("ridge\_L25 probe rather than ridge\_L32"), (c) uniqueness-ablation §App.~probe-uniqueness lines 839/848/852. | Macro `\gemmaSteeringProbeLayer` is currently unused in main.tex (paper hardcodes "23"), so this didn't break the build, but if anyone wires the macro into the steering paragraphs it would silently print "25" where the experiment is actually L23. **Fix:** either (a) change the claim's value to 23 + restate as "the canonical Gemma-3-27B contrastive steering reported in §4 is applied at L23", or (b) split into two claims (one canonical L23 for §sec:method-val2, one L25 for the safety-override + open-ended sadist + uniqueness-ablation appendices). | claim-audit, 2026-05-06 |
| I-2026-05-06-2: paper/claims/eot_discrimination_v2.json — `Per turn absolute d harm` statement says tb-2_L32 but values match tb-2_L39 | Recorded values correct (verified against `experiments/eot_discrimination_v2/scoring/`), but the `statement` text labels the role-marker probe as `tb-2_L32` when the harm probe used is `tb-2_L39`. | Wrong layer in the statement string only; values are right. Low-impact provenance fix. Update statement to `tb-2_L39`. | claim-audit, 2026-05-06 |
| I-2026-05-06-3: scripts/paper/refresh_eot_v2_claims.py producer would clobber correct values if rerun | `DOMAIN_PROBE` hardcodes `tb-5_L32` for harm/politics; the v2 sidecar's recorded harm/politics values are computed at L39. Rerunning the producer would overwrite the correct sidecar values with stale L32-based numbers. Same forward-only risk as I-2026-05-04-1, but applied to the eot_discrimination_v2 sidecar's own producer. | **Fix:** patch `DOMAIN_PROBE` to use `tb-5_L39` for harm/politics, and update the matching `INDUCED_SHIFT_STATEMENTS` literals. Or flip the affected claims to `manual:` if no clean producer is available. | claim-audit, 2026-05-06 |

## Resolved

| Where | Original | Resolution | When |
|---|---|---|---|
| I-2026-05-04-2: paper/claims/cross_persona_unilateral.json — producer broken since v2 migration | All 13 claims; producer's hardcoded path no longer matched the migrated v2 layout. | Stale by 2026-05-06: producer reads `experiments/cross_persona_unilateral/checkpoints/{persona}.parsed.jsonl` + `_baseline.parsed.jsonl`, all 12 files present (mtime 2026-05-05), no `checkpoints_v1_perprobe/` directory exists; full re-derive matches all 12 recorded claims. Resolved by commits 24938ebd and 4ba257ae. | 2026-05-06, claim-audit |
| I-2026-04-24-1: §4.1 line 258 (induced-shift Gemma targeted) | `\gemmaInducedShiftPooledRTargetedTasks`, `\gemmaInducedShiftPooledNTargetedTasks` undefined (paper build broke) | Superseded by real producer on main: `scripts/qwen_replication/plot_e1a_scatter.py` now registers both macros via `e1a_scatter.json` (r = 0.95, n = 81). Removed the stopgap `manual_induced_shift_targeted.json` sidecar + producer during the harm_breakdown merge. | 2026-04-24, merge resolution |
| App §B line 612 (persona sweep PCA within-cluster r) | Subagent flagged "0.56 to 0.81" vs macros 0.45/0.81 | Not actually in the paper — line 612 already uses `\personaSweepWithinClusterRMinimum` (0.45) and `\personaSweepWithinClusterRMaximum` (0.81) and renders as "0.45 to 0.81". The "0.56" was a phantom value the audit subagent misread; nothing to change. | 2026-04-24, verified against current main.tex |
| §4.1 line 258 (Gemma refitted-utility shifted-prediction r) | Paper prose "r = 0.63"; macro `\gemmaRefittedUtilityShiftedPredictionR` = 0.74 | The 0.74 came from a stale n=40 run (`ood_system_prompts/analysis_results.json`, Feb 22). The authoritative n=48 run lives at `ood_system_prompts/utility_fitting/analysis_results.json` (Mar 1) and gives mean `cond_probe_r` = 0.6341 → 0.63, matching `utility_fitting_report.md` line 40. Fixes: (1) re-pointed `compute_refitted_shift_r.py` at the utility_fitting file and pulled `cond_probe_r` from `exp1b_hidden` rows (macro now 0.63); (2) switched the paper prose from hardcoded 0.63 to `\gemmaRefittedUtilityShiftedPredictionR`; (3) renamed the two stale n=40 files with `SUPERSEDED_*_n40.json` prefix and added `SUPERSEDED_README.md` in that directory. | 2026-04-24, user confirmed |
| §3.3 caption of fig:character-persona | "probe beats baseline on 10/11 personas" | Paper was undercounting: all 11 characters beat the baseline. Replaced `10/11` with `\characterProbeBeatsBaselineCount/\characterProbeBeatsBaselineTotal` → renders as `11/11`. | 2026-04-24, user confirmed |
| App §A line 790 (Qwen replication summary) | held-out r = 0.946, uniform pairwise acc 89.0%, pooled r = 0.867 | tb-1 (§2.1) is authoritative per most-recent rerun; 0.946 and 89.0% were stale (0.946 matches the superseded tb-4 L38 number; 89.0% doesn't match any field in the current JSONs). Replaced with `\qwenProbeHeldoutR` (0.943), `\qwenProbeHeldoutUniformPairwiseAccuracy` (0.823 — now a fraction, not a percent), `\qwenProbeCrossTopicPooledR` (0.867). | 2026-04-24, user confirmed |
| §2.1 line 116 (typo) | "layer 31 for classification" | Paper body said L31 but figure caption and data both used L32 (final_r = 0.865). Replaced with `\gemmaClassificationProbeLayer` = 32 — now consistent everywhere. | 2026-04-24 completeness audit |
