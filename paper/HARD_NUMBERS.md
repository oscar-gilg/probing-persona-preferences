# Hard numbers — kept inline in `main.tex`

Numbers that resist macro-ification (too context-specific, formatting-heavy,
compound expressions that don't map cleanly to a single value). Each entry
should explain why a macro would be worse.

| Line | Numeral | Why inline |
|---|---|---|
| | | |
